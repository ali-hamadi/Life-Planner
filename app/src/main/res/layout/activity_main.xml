 <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="left"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.example.ali.newlifeplanner.MainActivity"
        android:id="@+id/relativelayout" >

    <ListView
        android:id="@+id/listView1"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_alignLeft="@+id/btnFixedTimeTask"
        android:layout_below="@+id/btnFixedTimeTask"
        android:textSize="2dp">
    </ListView>

    <Button
        android:id="@+id/btnFixedTimeTask"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="Create Task"
        android:textSize="14sp"
        android:drawableLeft="@drawable/custom_icon4"
        android:paddingRight="153dp"
        tools:ignore="HardcodedText"
        />

    </RelativeLayout>